#include "main.h"
/**
 *_atoi - function to convert a string tto an integer
 *@n: the first integer slash character
 * Return: 0
 */
int _atoi(char *s)
{
  int i, d, n, len, f, digit;
  i = 0;
  d = 0;
  n = 0;
  len = 0;
  f = 0;
  digit = 0;
  while (s[len] != '\
	 0')
}